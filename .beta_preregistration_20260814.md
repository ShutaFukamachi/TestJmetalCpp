# β 拡大 A/B テスト 事前登録（2026-08-14, 結果を見る前に固定）

## 対象
- インスタンス: j3034_1, j309_1, j3031_1, j3041_1（Part1と同一4件、ms_gap高低で多様性確保）
- 条件: RR{0.00,0.25,0.50}×RV{0,1} の6条件（RCPSP_STANDARD_CONDITIONS、production標準）
- P1のみ（--p1-only）。P2/P3は対象外（窓上限の検証はP1で完結するため）
- 候補: β ∈ {0.1, 0.25, 0.75}。現行production既定 β=0.5 が比較対象（ベースライン）
- 評価予算・popSize等は現行productionと同一（変更しない）

## 反復回数
- ベースライン(β=0.5): 4反復（独立乱数シード）→ ノイズ床推定
- 各候補(β=0.1/0.25/0.75): 2反復ずつ

## 採否判定指標（優先順位）
1. **HV比**（NSGA前線 / MIP前線、共有nadir=(1.1,1.1)、gap48_analysis.pyのhypervolume_2d/normalize_frontを再利用）が主指標。
   - 採用条件: 候補のHV比平均 − ベースラインHV比平均 > ベースラインのノイズ床（反復間レンジ）
2. コストgap平均が**悪化しないこと**（悪化幅がコストgapのノイズ床以内であること）
3. 実行時間: 現行の数倍を超えないこと（スモークテストで+6.7%を確認済み、閾値未到達）

## 決定ルール
- 複数のβが採用条件を満たす場合、**最小のβ**を採用候補とする（探索空間を不必要に広げない）
- 端点到達数など補助指標は判定に使わない（A2 Phase2の教訓: 端点改善は前線全体の質と一致しない）
- 結果を見てから基準を変更しない

## 「窓を広げたのに解の質が上がらない」場合の切り分け
Part1で表現可能率が理論上限(85.5%)まで飽和することを確認済み（β=0.75で到達）。
もしβ=0.75でHV比が改善しない場合、それは「表現力の問題」ではなく
「表現力はあるが探索がその領域を見つけられない（探索空間拡大によるコスト）」
＝**探索の問題**と結論する。
